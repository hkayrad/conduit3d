export enum UserRoles {
    ADMIN = "admin",
    USER = "user",
}

export enum HatCinsi {
    HAVAI = "HAVAİ",
    BARA = "BARA",
    YERALTI = "YERALTI"
}

export enum DataType {
    BUILDING = "building",
    TRAFO = "trafo",
    POLE = "pole",
    LINE = "line",
    REKORTMAN = "rekortman"
}